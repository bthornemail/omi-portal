## The Hidden Five: Why the Five-Fold Root Is Not a Visible Step

The five-fold layer is the ontological root of the OMI packet model:

```text
5! = 120 = 2³ × 3 × 5
```

This is the first layer where the factor `5` appears explicitly as a rooted packet core. In OMI terms:

```text
5! = packet root
5! = five-cell semantic closure
5! = hidden simplex identity
```

However, once the system climbs above the five-fold root, the factor `5` stops appearing as a visible operational step. It becomes encapsulated inside the 240-state bridge:

```text
240 = 2 × 5!
240 = 2 × 120
240 = 16 × 15
240 = 16 × (3 × 5)
```

So the factor `5` remains present, but it is no longer exposed alone. It is carried inside `240`.

This resolves the apparent tension with the decimal carrier of `1/73`:

```text
1 / 73 = 0.01369863...
```

The period block is:

```text
B = [0, 1, 3, 6, 9, 8, 6, 3]
```

The visible stepping digits contain no `5`.

That is not a contradiction. It is a structural clue.

The factor `5` is not walked as a decimal digit. It is held as the hidden packet root. The operational climb walks the period-8 carrier produced by the Δ_C law and the prime-73 recovery structure, while the five-fold packet identity remains presupposed underneath the bridge.

Therefore:

```text
5 is not a moving pointer.
5 is the rooted packet simplex.
```

The five-fold rule is not something the decimal carrier steps to. It is the condition that makes the climb meaningful.

---

## Root Versus Step

OMI must distinguish between **root** and **step**.

A root is a structural presupposition.

A step is an operational transition.

The five-fold layer is a root:

```text
5! = 120
```

The four-fold layer is a visible selector/projection surface:

```text
4! = 24
```

The 240-state bridge connects them:

```text
2 × 5! = 15 × 16 = 240
```

This means:

```text
five-fold root
→ orientation/complement
→ 240-state bridge
→ four-fold visible selector
```

The five-fold root is verified as packet identity. The four-fold selector is where that identity becomes visible, addressable, and projectable through FS/GS/RS/US or an equivalent channel surface.

Thus:

```text
Five-fold rules are presuppositional.
Four-fold facts are projective.
```

A five-fold rule protects the hidden packet root.

A four-fold fact exposes a grounded face of that root.

---

## Revised Rule/Fact Interpretation

The rule/fact transition should therefore be written as:

```text
Five-fold rule
  = hidden packet-root obligation

Four-fold fact
  = visible selector/projection grounding
```

The five-fold rule is verified once as a root condition:

```text
packet is closed
packet is coherent
packet preserves five-cell identity
packet can enter the 240 bridge
```

After that, the four-fold selector projects the packet into visible state:

```text
FS = file/frame/storage
GS = group/graph/global
RS = relation/routing/record
US = unit/text/terminal
```

So the operational derivation is:

```text
5! root packet
→ ×2 orientation/complement
→ 240 oriented packet surface
→ 15 × 16 active nibble/carrier surface
→ 4-fold selector projection
→ 3 × 240 S-P-O semantic sweep
→ 7 × 3 × 240 Fano replay ring
```

The five-fold root is not lost. It is hidden inside every higher layer.

---

## Canonical Hidden-Root Formula

The corrected bridge law is:

```text
5! is rooted.
240 carries it.
4! exposes it.
6! sweeps it.
7! replays it.
```

Expanded:

```text
5!       = 120                       hidden packet root
2 × 5!   = 240                       oriented packet bridge
15 × 16  = 240                       visible byte/nibble selector surface
3 × 240  = 720 = 6!                  S-P-O semantic sweep
7 × 720  = 5040 = 7!                 Fano replay ring
```

The factor `5` appears explicitly only at the root. Above that root, it is encapsulated inside `240`.

This is why the period-8 decimal carrier can omit the digit `5` while still preserving the five-fold structure.

---

## Final Hidden-Root Statement

The five-fold packet root is present but silent.

It is not a digit in the visible carrier. It is not stepped to by the `1/73` period block. It is the simplex foundation that the carrier presupposes.

The four-fold layer is the first visible face of that hidden root. It converts rooted packet identity into selectable, addressable, projectable state.

Therefore:

```text
5-fold = hidden root
4-fold = visible face
240 = bridge
```

And the complete OMI rule/fact interpretation becomes:

```text
Five-fold rules preserve hidden packet identity.
Four-fold facts expose that identity through visible selector surfaces.
The 240-state bridge is where hidden root and visible face become the same operational state.
```

